<?xml version="1.0" encoding="utf-8"?>
<!--
    Splash screen branding image (API 31+, windowSplashScreenBrandingImage).
    References the PNG in drawable-nodpi.  antialias + gravity are not valid
    attributes for <bitmap> in all build-tool versions; use the minimal form.
-->
<bitmap xmlns:android="http://schemas.android.com/apk/res/android"
    android:src="@drawable/mangaworld_logo" />
