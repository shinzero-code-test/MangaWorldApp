{
  "rules": {
    "chatRooms": {
      "$roomId": {
        ".read": "auth != null",
        "messages": {
          "$messageId": {
            ".write": "auth != null"
          }
        }
      }
    }
  }
}
