{
  "rules": {
    "chatRooms": {
      "$roomId": {
        ".read": "auth != null",
        "messages": {
          "$messageId": {
            ".write": "auth != null && !data.exists() && newData.child('authorUid').val() == auth.uid"
          }
        }
      }
    }
  }
}
